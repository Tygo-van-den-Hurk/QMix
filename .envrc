# shellcheck shell=sh

# automatically loads a Nix Development environment if nix is installed.
if has nix; then
  use flake .
else
  echo "Nix not found, obviously required for this repository?"
  exit 1
fi
